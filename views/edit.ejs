<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Essay</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('sidebar.ejs') %>

    <div class="content">
        <h2>✂ Edit Essay</h2>

        <form method="POST" action="/edit/<%= essay.id %>">
            <label for="title">Update Title:</label>
            <br>
            <input type="text" id="title" name="updatedTitle" value="<%= essay.title %>" class="input-field">

            <label for="content">Update Content:</label>
            <br>
            <textarea id="content" name="updatedContent" class="text-area"><%= essay.content %></textarea>

            <div class="button-container">
                <button type="submit">Save Changes</button>
                <a href="/Essays" class="cancel-btn">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>
