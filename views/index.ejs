<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Essays</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body>
    <%- include('sidebar.ejs') %>

    <div class="content">
        <h1>📖 Essays</h1>
        <h2>Find what you are interested in. Write what you are interested in.</h2>

        <div class="essay-container">
            <% essays.forEach((essay) => { %>
                <div class="essay-card">
                    <h6 class="essay-title"><u><%= essay.title %></u></h6>
                    <p class="essay-content"><%= essay.content.substring(0, 100) %>...</p>
                    <div class="button-container">
                        <form action="/edit/<%= essay.id %>" method="get">
                            <button type="submit">Edit</button>
                        </form>
                        <form action="/read/<%= essay.id %>" method="get">
                            <button type="submit">Read</button>
                        </form>
                        <form action="/delete/<%= essay.id %>" method="get">
                            <button type="submit">Delete</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>

</html>
